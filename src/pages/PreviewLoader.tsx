import PortalSkeleton from "@/components/PortalSkeleton";

/**
 * /preview/loader — diagnostic preview of the Feb 14 ghost-car loader.
 * This is the official loader used during VIN/info lookups and photo uploads.
 */
const PreviewLoader = () => {
  return <PortalSkeleton />;
};

export default PreviewLoader;
